"""App configuration for the assessments application."""
from django.apps import AppConfig


class AssessmentsConfig(AppConfig):
    """Configuration for the assessments app."""
    name = "assessments"
