/**
 * Placeholder hook, not implemented. Returns an empty object and currently
 * has no consumers anywhere in the app - the simulation flow's actual
 * state/data-fetching lives ad hoc inside pages/SimulationQuestions.jsx
 * and pages/Score.jsx instead.
 * @returns {{}}
 */
export default function useSimulation() {
  return {};
}
